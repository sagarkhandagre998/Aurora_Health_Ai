# Only ignore installed dependencies — everything else is committed
# so the project can be cloned and worked on from another machine.
# Run `npm install` after cloning to rebuild node_modules.
node_modules/
